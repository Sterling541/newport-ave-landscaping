{
  "version": 2,
  "buildCommand": "pnpm install && pnpm build",
  "builds": [
    {
      "src": "server/vercel-entry.ts",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb",
        "includeFiles": ["dist/**", "client/index.html"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server/vercel-entry.ts"
    }
  ]
}
